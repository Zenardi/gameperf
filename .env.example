# gameperf monitoring stack — local configuration
#
# HOST_IP: the LAN/Wi-Fi IP of this machine so that Prometheus (running in
# Docker) can reach the gameperf metrics server running on the host.
#
# Find your IP:
#   hostname -I | awk '{print $1}'
#
HOST_IP=192.168.1.x
